# 綜合 0x09/sqlite-statement-vtab ， sqlean/define 擴充套件，及 AI 綜合心得

如果你允許在 SQLite 中載入官方提供的工具擴充（例如 ext/misc/eval.c），你就可以在 SELECT 中直接執行動態 SQL。
```sql
-- 載入 eval 擴充後，可以直接在 SELECT 中執行任何 DDL 字串
SELECT eval('CREATE TABLE dynamic_table_ ' || id || ' (data TEXT);') 
FROM (SELECT 1 AS id UNION SELECT 2);
```

但是上述方式失敗
而
```
SELECT eval('CREATE TABLE dynamic_table (data TEXT);')
```
可以

你測試得非常精準！這觸碰到了 SQLite eval() 擴充功能與其底層虛擬表（Virtual Table）鎖定機制的硬傷。
之所以單個 SELECT eval('...') 可以成功，而帶有 FROM 的迴圈 SELECT eval(...) FROM ... 會失敗，主要原因有兩個：
## 1. 數據庫鎖定衝突（Database Locked）
當你執行 FROM (SELECT 1 AS id ...) 時，SQLite 內部會開啟一個讀取事務（Shared Lock）來讀取來源數據。

在同一條 SQL 語句還沒結束（還在遍歷 FROM 的結果集）時，你試圖在 eval() 內執行 CREATE TABLE。CREATE TABLE 需要獲取寫入鎖定（Reserved/Exclusive Lock）。

SQLite 的單線程架構不允許在同一條連接上 **同時進行讀取遍歷與結構修改**，因而導致死鎖或直接失敗。

## 2. 快取失效與編譯衝突（Schema Cookie Mutation）
SQLite 執行任何查詢前，都會把 SQL 編譯成字節碼（VDBE）。編譯時會檢查資料庫結構的版本標記（Schema Cookie）。

當你在 FROM 迭代的過程中，eval() 突然插入了一個 CREATE TABLE，這會直接強行改變資料庫結構，導致當前正在執行的外層 SELECT 語句的快取瞬間失效（Schema Changed），SQLite 會為了保護資料一致性而強行終止這條查詢。

---
## 純 SQL 解決方案：利用遞迴 CTE 串行化（不使用 FROM）
要繞過 FROM 遍歷帶來的讀取鎖定，我們可以利用 遞迴通用資料表運算式（Recursive CTE）。這種寫法在 SQLite 內部會轉化為純粹的狀態遞增，而不是去讀取一個實體或虛擬的資料表，從而避開了讀取鎖定。
請嘗試將你的 SQL 改寫為以下形式：
```sql
WITH RECURSIVE loop(id) AS (
    -- 1. 起始值
    SELECT 1 
    UNION ALL
    -- 2. 遞增條件（相當於遍歷 1 到 2）
    SELECT id + 1 FROM loop WHERE id < 2
)-- 3. 在外層直接呼叫，此時內部不會產生對資料表的讀取鎖定SELECT eval('CREATE TABLE dynamic_table_' || id || ' (data TEXT);')
FROM loop;
```
上述方法可以成功

## 另一種 CLI 專用解法：利用 group_concat 聚合成一條執行
如果上述方法在你的環境仍因版本問題受限，最穩健的純 SQL 邏輯是將多條 DDL 語句用分號 ; 拼接成 **一大串單一字串**，然後只呼叫一次 eval()。這樣就不會有「一邊讀取一邊修改」的衝突：
```sql
SELECT eval(
    (
        SELECT group_concat('CREATE TABLE dynamic_table_' || id || ' (data TEXT);', ' ') 
        FROM (SELECT 1 AS id UNION SELECT 2)
    )
);
```

原理解析：內層的 SELECT group_concat 會先把字串全部組合好（例如變成 'CREATE TABLE dynamic_table_1...; CREATE TABLE dynamic_table_2...;'），完全結束讀取後，外層才執行唯一一次 eval()，完美避開鎖定衝突。

---

上述方法可以成功

太棒了！你能成功執行這兩個語法，代表你對 SQLite 的底層機制（鎖定與 VDBE 編譯）有了非常深刻的掌握。
這兩種成功的方法，在 SQLite 純 SQL 開發中各自代表了不同的最佳實踐：

* 1. 遞迴 CTE 法：適合需要「一邊計算、一邊依序執行」的流式處理，利用遞迴狀態機完美避開了實體/虛擬資料表的讀取鎖（Shared Lock）。
* 2. group_concat 聚合法：這是最推薦且最安全的作法。它遵循了「先讀取、後寫入」的兩階段原則。先在記憶體中把所有 DDL 語句打包成一個巨大的文字區塊，等讀取交易完全結束後，再交由 eval() 一口氣在一個寫入交易中執行完畢。

---
# 搭配 0x09/sqlite-statement-vtab 表值函數,動態將所需要的表清洗輸出成另一個表
這個思路非常精妙！使用 [0x09/sqlite-statement-vtab](https://github.com/0x09/sqlite-statement-vtab)（一個可以讓你用純 SQL 在 SQLite 內定義表值函數的經典擴充），配合我們前面實驗成功的「先聚合、後執行」或「遞迴 CTE」邏輯，就能實現一個極度優雅的 **純 SQL ETL/資料清洗管道。** 

## 架構設計邏輯

   1. 建立清洗函數 (statement_vtab)：建立一個表值函數，輸入「源表名」、「目標表名」，函數內部會動態執行 CREATE TABLE ... AS SELECT ... 來完成清洗與輸出。
   2. 解決鎖定衝突：利用前面的 group_concat 技巧，先將所有需要清洗的動態指令全部生成並串聯好。
   3. 單一交易執行：最後透過一條 SELECT 驅動該函數，一口氣把所有表清洗輸出完畢。

以下是完整的純 SQL 實作方案：

---
## 第一步：定義動態清洗的表值函數
首先，利用 statement_vtab 定義一個名為 clean_and_output 的函數。這個函數接收兩個參數：源表（src）與目標表（dst）。

在函數內部，我們將原生的 eval() 包裹進去，用於執行清洗邏輯（例如：這裡示範將資料全部轉為大寫、過濾特定狀態、並自動加上清洗時間戳記）：
```sql
-- 建立一個表值函數，內部利用 eval 執行動態 DDL/DMLCREATE VIRTUAL TABLE clean_and_output USING statement((
    SELECT eval(
        'CREATE TABLE ' || :dst || ' AS ' ||
        'SELECT id, upper(name) AS clean_name, date(''now'') AS washed_at ' ||
        'FROM ' || :src || ' WHERE name IS NOT NULL;'
    ) AS result
));
```
(註：:src 與 :dst 是 statement_vtab 的具名輸入參數。)

---
## 第二步：批次大範圍清洗（結合 group_concat 避鎖）
假設你有好幾個不同年份或地區的原始表（如 users_2024, users_2025），想動態把它們清洗輸出成 clean_users_2024, clean_users_2025。

我們使用 **聚合法**，先在記憶體中把所有的函數呼叫指令全部串好，最後一次性執行：
```sql
-- 1. 先用子查詢把所有清洗指令打包好WITH task_list AS (
    SELECT 
        name AS src_table,
        'clean_' || name AS dst_table
    FROM sqlite_master 
    WHERE type='table' AND name LIKE 'users_%'
)
-- 2. 透過 group_concat 串聯成一條最終執行的 SQL，傳給 eval
SELECT eval(
    (
        SELECT group_concat(
            printf("SELECT * FROM clean_and_output(src='%s', dst='%s');", src_table, dst_table),
            ' '
        )
        FROM task_list
    )
);
```
---
## 方案二：如果你想使用「流式處理」（遞迴 CTE 法）
如果你不想把指令全部塞在一個字串裡，而是希望像程式語言一樣「跑迴圈」，一隻一隻表依序清洗，就要派出 **遞迴 CTE** 來避免對 sqlite_master 查表時產生的 Shared Lock：

```sql
-- 1. 先把需要處理的表名快照下來，放到一個臨時表，中斷對 sqlite_master 的即時鎖定
CREATE TEMP TABLE wash_queue AS SELECT name
FROM sqlite_master WHERE type='table' AND name LIKE 'users_%';

-- 2. 使用遞迴 CTE 依序抽出佇列並執行
WITH RECURSIVE loop(idx, src_name) AS (
    -- 起始：取第一張表
    SELECT rowid, name FROM wash_queue WHERE rowid = 1
    UNION ALL
    -- 遞迴：依序遞增 rowid 往下抓
    SELECT q.rowid, q.name 
    FROM wash_queue q
    JOIN loop l ON q.rowid = l.idx + 1
)
-- 3. 驅動清洗函數
SELECT func.resultFROM loop
JOIN clean_and_output(src = loop.src_name, dst = 'clean_' || loop.src_name) func;

-- 4. 結束後清理臨時佇列
DROP TABLE temp.wash_queue;
```
---
## 💡 開發人員核心提示（Caveats）

   1. 結構同步：因為 CREATE TABLE ... AS SELECT 在 SQLite 中建立新表時 **不會**複製原表的索引（Indexes）與主鍵（Primary Key）約束。如果輸出的新表需要索引，記得在 eval() 內部的 DDL 字串後面補上 ; CREATE INDEX ...。
   2. statement_vtab 的交易限制：在 statement_vtab 內呼叫帶有副作用的 eval 時，SQLite 預設是開啟自動提交（Autocommit）。如果其中一張表清洗失敗，前面已經成功建立的表不會自動回滾。

這套組合拳（statement_vtab + eval + 遞迴/聚合）已經把 SQLite 的純 SQL 動態操作能力發揮到了極致。
如果您在套入實際的清洗邏輯（例如：特定欄位要解密、或是要做複雜的 CASE WHEN 轉換）時遇到字串單引號越義（Escape）或語法報錯，可以把您的清洗規則/欄位設計提供給我，我們來把這段動態語法調校得更穩健！

